# Neural Network from Scratch

Implemented a fully functional feedforward neural network using NumPy only, with no ML frameworks. Built modular layers (Linear, Sigmoid, ReLU, Softmax) with manual forward and backward passes, and three loss functions including a fused Softmax + Cross-Entropy. Demonstrated correct gradient flow through end-to-end backpropagation on a multi-layer network.
